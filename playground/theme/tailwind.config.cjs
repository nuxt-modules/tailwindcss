const colors = require('tailwindcss/colors')

module.exports = {
  theme: {
    extend: {
      colors: {
        brand: colors.fuchsia['500'],
        accent: colors.fuchsia['500']
      }
    }
  }
}
